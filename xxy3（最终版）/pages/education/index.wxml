<!-- education/index.wxml -->
<view class="container">
  <view class="header-section">
    <text class="title">教育公益</text>
    <text class="subtitle">服务社会，奉献爱心</text>
  </view>

  <view class="activity-list">
    <view 
      class="activity-item" 
      wx:for="{{activityList}}" 
      wx:key="id"
      bindtap="viewDetail"
      data-id="{{item.id}}"
    >
      <view class="activity-icon">🎓</view>
      <view class="activity-content">
        <text class="activity-title">{{item.title}}</text>
        <text class="activity-location">📍 {{item.location}}</text>
        <text class="activity-date">📅 {{item.date}}</text>
        <text class="activity-participants">👥 {{item.participants}}人参与</text>
      </view>
      <view class="activity-arrow">›</view>
    </view>
  </view>

  <view class="empty-state" wx:if="{{activityList.length === 0}}">
    <text class="empty-icon">📚</text>
    <text class="empty-text">暂无活动</text>
  </view>
</view>

